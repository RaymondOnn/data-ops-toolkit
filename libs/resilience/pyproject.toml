[project]
name = "resilience"
version = "0.1.0"
description = "Shared I/O and Database clients"
dependencies = [
    "sdnotify",
]
requires-python = ">=3.11"

[tool.setuptools]
packages = ["resilience"]
package-dir = { "resilience" = "." }
