[project]
name = "utils"
version = "0.1.0"
description = "Shared I/O and Database clients"
dependencies = [
    "loguru",
    "pendulum",
    "psutil>=5.8.0"
]
requires-python = ">=3.11"

[tool.setuptools]
packages = ["utils"]
package-dir = { "utils" = "." }