[project]
name = "auth"
version = "0.1.0"
description = "Shared I/O and Database clients"
dependencies = [
    "cryptography",
    "boto3",
]
requires-python = ">=3.11"

[tool.setuptools]
packages = ["auth"]
package-dir = { "auth" = "." }
