[project]
name = "metaclasses"
version = "0.1.0"
description = ""
dependencies = [
    "autoregistry"
]
requires-python = ">=3.11"

[tool.setuptools]
packages = ["metaclasses"]
package-dir = { "metaclasses" = "." }
