[project]
name = "clients"
version = "0.1.0"
description = "Shared I/O and Database clients"
dependencies = [
    "polars",
]
requires-python = ">=3.11"

[tool.setuptools]
packages = ["clients"]
package-dir = { "clients" = "." }