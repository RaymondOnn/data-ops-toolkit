job:
  name: "test_job"
  raw:
    num_partitions: 1
  archive:
    enable_archival: true
    service_ref: "standard_archive"

datasets:
  orders:
    num_partitions: 1
    extract:
      load_mode: "snapshot"
      service_ref: "flat_file"
      source_identifier: "samples/sample_orders.csv"
      source_params:
        url: "file:///Users/main/Documents/Code/Github/data-ops-toolkit/apps/ingestion"
    load:
      service_ref: "clickhouse_db"
      sink_identifier: "orders"
    partition_value: "2023-10-27"
    archive:
    enable_archival: true

# issue: we assign partition_value for snapshot but filter based on partition value for delta