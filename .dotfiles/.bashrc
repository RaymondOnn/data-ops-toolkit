# Ensure user-local binaries are reachable
export PATH="$HOME/.local/bin:$PATH"

# Optional: Alias 'just' to your project wrapper for total consistency
alias just='/Users/main/Documents/Code/Github/data-ops-toolkit/justw'
